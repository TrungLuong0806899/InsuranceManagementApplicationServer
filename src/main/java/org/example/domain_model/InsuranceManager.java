package org.example.domain_model;

public class InsuranceManager extends Customer{
    public InsuranceManager(String id, String fullName, String phoneNumber, Address address, String email, String password) {
        super(id, fullName, phoneNumber, address, email, password);
    }
}
